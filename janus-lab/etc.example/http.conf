; El servidor HTTP de Asterisk viene apagado y ARI va montado encima.
; Se añaden enabled y bindport al [general] que ya existe.

[general]
servername=Asterisk
enabled=yes
bindport=8088
bindaddr=127.0.0.1      ; solo local; para exponerlo hace falta más que cambiar esto
